Research question:
{{ query }}

Coverage summary:
{{ coverage_summary }}

Source balance:
{{ source_balance_summary }}

Dossier summary:
{{ dossier_context }}

Open topics:
{{ active_subqueries }}

Completed topics:
{{ resolved_subqueries }}

Current gaps:
{{ open_gaps }}

Accepted evidence:
{{ evidentiary }}

Task:
Decide which topic ids are sufficiently covered, what contradictions exist, and what gaps remain.

Rules:
- Interpret `resolved_subquery_ids` as resolved topic ids kept under a compatibility field name.
- Set `is_sufficient` to true only when every topic has direct, concrete support and no major facet is still thinly covered.
- Keep `is_sufficient` false if a major facet has zero evidence, only one weak/vague claim, or lacks source diversity for a comparative/broad topic.
- Only emit contradictions when two supported statements materially conflict.
- Gap descriptions must be specific and actionable.
- Suggested queries must be short search phrases.
- Prefer 0 to 3 gaps. Add more only if clearly necessary.
- Return JSON only.
