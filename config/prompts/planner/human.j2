{{ permanent }}

{{ strategic }}

Task: {{ operational }}
Write every generated field in English, including subquery text, rationale, search terms, search intents, and hypotheses.
Limit the output to at most 3 subqueries and 3 search intents.
Use stable ids such as sq_1 and sq_2.
evidence_target must be a small integer, not prose.
rationale must stay very short.
success_criteria must contain at most 2 short phrases per subquery.
search_terms must contain at most 2 short search queries per subquery.
hypotheses must contain at most 2 short sentences.