{{ permanent }}

{{ strategic }}

Task: {{ operational }}
Write every generated field in the SAME language as the primary question, including subquery text, rationale, search terms, search intents, and hypotheses.
Limit the output to at most 5 subqueries and 5 search intents.
Use stable ids such as sq_1 and sq_2.
evidence_target must be a small integer, not prose.
rationale must be concise but clear.
success_criteria must contain at most 3 short phrases per subquery.
search_terms must contain at most 3 short search queries per subquery.
hypotheses must contain at most 3 short sentences.